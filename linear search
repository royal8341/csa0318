#include<stdio.h>
int main()
{
	int a[]={1,2,3,4,5};
    int n=sizeof a/sizeof(a[0]);
    int key=3;
    int flag=0;
    for(int i=0;i<n;i++)
    {
    	if(key==a[i]){
		
    	printf("position of %d of %d",key,i);
    	
}
}
}
